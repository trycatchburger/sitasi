<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /sitasi/public/  # Sesuaikan dengan path subdirektori Anda
    
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . index.php [L]
</IfModule>